document.getElementById('contactForm').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Form submitted!');
    // You can add your form submission logic here, e.g., send the data via AJAX.
});
